package com.example.kotlin_algorithm.implementation

import java.util.Scanner

fun main() = with(Scanner(System.`in`)) {
    println(nextLine() + "??!")
    /**
     * TODO nextLine() 다른 사람들은 어떻게 쓰는지 찾아보기
     */
}